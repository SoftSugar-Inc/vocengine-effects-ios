# Uncomment the next line to define a global platform for your project
# platform :ios, '14.0'

source 'https://github.com/volcengine/volcengine-specs.git'

target 'SampleHS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SampleHS
  pod 'TTSDKFramework', '1.46.2.11-premium', :subspecs => ['LivePush']
#  pod 'TTSDKFramework', '1.45.3.6-premium', :subspecs => ['LivePush']
#  pod 'TTSDKFramework', '1.45.3.6-premium', :subspecs => ["LivePush"]
#  pod 'TTSDKFramework', '1.46.2.11-premium', :subspecs => ['LivePush-RTS']

end

#post_install do |installer|
#  installer.pods_project.targets.each do |target|
#    target.build_configurations.each do |config|
#      # 给所有 target 加上 CoreServices.framework
#      config.build_settings['OTHER_LDFLAGS'] ||= ['$(inherited)']
#      config.build_settings['OTHER_LDFLAGS'] << '-framework CoreServices'
#    end
#  end
#end
